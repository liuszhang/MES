@model DashboardSourceModel

@Html.DevExpress().DashboardViewer(settings => {
    settings.Name = "dashboardViewer1";
    settings.CallbackRouteValues = new { Controller = "Samples", Action = "ChampionsLeagueStatisticsPartial" };
    settings.ExportRouteValues = new { Controller = "Samples", Action = "ChampionsLeagueStatisticsPartialExport" };
    settings.AllowExportDashboardItems = true;
    settings.FullscreenMode = true;
}).BindToSource(Model).GetHtml()
